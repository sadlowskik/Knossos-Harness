# Knossos Field Atlas model-emblem mockup v9

Mode: built-in image generation, targeted edit of flat Atlas v8.

Preserve the full Atlas layout and replace human portraits and generic on-map agent markers with the model or endpoint emblem currently powering each user-named agent. Keep `MARCUS`, `VITUS`, `LYRA`, `CATO`, `OCTAVIA`, and `FELIX`, while adding subordinate source labels such as `ORNITH 35B`, `QWEN`, `DEEPSEEK`, `NEMOTRON`, `LOCAL`, and `CLOUD`.

Use the same emblem in the roster, on-map marker, and selected-agent inspector. Add `MODEL ornith-35b-a3b` and `SOURCE HUGGING FACE` to MARCUS. Imply this resolution order: Hugging Face repository avatar, endpoint/provider icon, user-uploaded emblem, generated initials fallback.

Match the flat Atlas design with restrained, memorable graphic marks. Avoid faces, Roman symbolism, generic AI brains, glossy app icons, or oversized provider branding.
