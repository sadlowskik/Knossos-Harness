# Knossos Field identity settings mockup v8

Mode: built-in image generation, targeted edit of the Senate model-emblem mockup.

Preserve the Roman world, routes, projects, Senate roster, top navigation, and portrait-plus-model badges. Dim the world and open a right-side `FIELD SETTINGS` drawer with `WORLD`, `IDENTITY`, and `MODELS` tabs; select `IDENTITY`.

Expose presentation controls for `THEME` (`ATLAS` or `ROME`), `AGENT IDENTITY` (`PORTRAIT`, `MODEL`, or `BOTH`), and `MAP MARKERS` (`PERSON`, `MODEL`, or `BOTH`).

Expose emblem resolution using `AUTO — HUGGING FACE` and the priority `HF AVATAR › ENDPOINT › UPLOAD › INITIALS`, with preview, upload, and reset controls.

For selected agent `MARCUS`, show fields for display name `Marcus`, engine alias `OX Alpha`, underlying model `GLM 5.3 Flash`, lineage `Distillation teacher`, enabled `USE TRACES FOR DISTILLATION`, and repository `zai-org/GLM-5.3-Flash`. Summarize the relationship as `OX ALPHA — powered by GLM 5.3 Flash — distillation traces enabled`.

Expose checked `TERMINAL`, `GIT`, `DOCKER`, and `BROWSER` tool authority with the explanation `Equipped tools define allowed calls.` Finish with `RESTORE DEFAULTS` and `SAVE SETTINGS`.

Keep the settings aligned, compact, and native to the premium Roman Cameo interface. Avoid a central modal, generic SaaS cards, excessive ornament, fake code, or verbose copy.
