# Knossos Field Senate model-emblem mockup v7

Mode: built-in image generation, targeted edit of Senate-world v6.

Preserve the Roman world and Senator portraits. Add a small model or endpoint emblem to each portrait, roster card, and map marker. Include a compact `IDENTITY BOTH` control representing `PORTRAIT`, `MODEL`, and `BOTH` modes.

Keep Senator names persistent while their current engines remain swappable. Add subordinate labels: `ORNITH 35B`, `QWEN`, `DEEPSEEK`, `NEMOTRON`, `LOCAL`, and `CLOUD`. Add `MODEL ornith-35b-a3b` and `SOURCE HUGGING FACE` to the selected MARCUS inspector.

Frame repository and endpoint emblems as small bronze office seals that complement rather than overpower Senator identity. Use the same source-resolution order as Atlas and preserve every existing tool, route, project, and progression mechanic.
