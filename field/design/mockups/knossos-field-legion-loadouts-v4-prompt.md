# Knossos Field legion-loadouts mockup v4

Mode: built-in image generation, targeted edit of adaptive-world v3.

Preserve the clean full-map layout and operator-grown Capital. Add restrained Roman civic language—arches, aqueduct-like service links, forum courtyards, standards, milestone roads, tiled roofs, bronze fasteners, and disciplined settlement plans—while retaining the operator-specific Cameo architecture. Target approximately 35% Roman civic grammar and 65% personalized project world.

Use cleaner 2D/2.5D strategy art with shallower perspective, simplified geography, matte texture, strong silhouettes, and fewer decorative objects.

Agents are user-named stylized field units. Select `MARCUS`; also show `VITUS`, `LYRA`, and `CATO`. The inspector shows `Validate local inference`, `62%`, and an equipment loadout containing `TERMINAL`, `GIT`, `DOCKER`, and `BROWSER`. Tool equipment represents the agent's real permissioned tool calls. Display `Docker health check passed` as a live action.

Show three project states: foundations/scaffolding, active workshop, and established civic complex. Growth represents verified contribution rather than XP. The selected project can expose `COMPLETE`, `VERIFIED`, and `PERSISTED` gates.

Avoid a full historical simulation, photorealism, heavy 3D, uncanny miniature people, sci-fi clutter, giant temples, medieval fantasy, generic AI imagery, resource currencies, excessive labels, or copied game assets.
