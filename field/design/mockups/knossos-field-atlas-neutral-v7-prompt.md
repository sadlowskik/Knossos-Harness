# Knossos Field Atlas-neutral mockup v7

Mode: built-in image generation, style-transfer edit of Senate-world v6.

Create a non-Roman `Atlas` theme while preserving the exact product structure, map state, hierarchy, agent identities, selected route, projects, delegated tools, maturity tiers, and information density from v6.

Replace the Mediterranean Roman world with an adaptive fictional operational atlas. The main project is the central `CAPITAL`; other projects are regions; websites are edge gateways; local services become infrastructure campuses and depots; remote providers become endpoint stations; model and research work become knowledge complexes. Developed geography exists only where real work exists.

Replace senators with clean user-named agent emblems or understated portraits. Use `MARCUS`, `VITUS`, `LYRA`, `CATO`, `OCTAVIA`, and `FELIX`. The bottom roster is labeled `AGENTS`.

The selected inspector shows `MARCUS`, objective `Validate local inference`, project `Cameo Runtime`, progress `62%`, and modern recognizable `TERMINAL`, `GIT`, `DOCKER`, and `BROWSER` tools, plus `Docker health check passed`.

Use premium neutral product design crossed with a stylized illustrated operations map: flat 2D/2.5D terrain, shallow depth, crisp silhouettes, subtle painted texture, graphite shell, warm gray land, slate-blue water, moss and clay accents, and Cameo ember-orange highlights. Add a tiny top-right setting reading `THEME  ATLAS`.

Remove every Roman, imperial, military, fantasy, and historical element. Avoid cyberpunk, neon AI imagery, photoreal satellite terrain, generic SaaS dashboards, glass cards, charts, territory polygons, currencies, cartoon city builders, or copied game assets.
